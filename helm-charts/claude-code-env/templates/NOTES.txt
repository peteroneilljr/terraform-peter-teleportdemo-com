Claude Code environment deployed successfully!

Node name registered with Teleport: {{ include "claude-code-env.nodeName" . }}

To connect to this environment via Teleport SSH:
  tsh ssh root@{{ include "claude-code-env.nodeName" . }}

Or to get an interactive shell:
  tsh ssh claude@{{ include "claude-code-env.nodeName" . }}

Required values that must be set:
  - teleport.proxyAddr       Teleport proxy address (e.g. peter.teleportdemo.com:443)
  - image.repository         Container image registry/name
  - anthropicApiKey.secretName  Kubernetes secret containing the Anthropic API key

The TeleportProvisionToken "{{ .Values.teleport.botTokenName }}" must be pre-created
in the Teleport cluster before this chart is deployed.
